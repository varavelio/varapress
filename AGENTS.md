# Agent Context — VaraPress

## Summary

**VaraPress** is the official Zola theme for Varavel, designed to build landing pages, blogs, and documentation sites. It is a Zola theme (not a standalone site), built with TailwindCSS v4 and self-hosted Geist typography. It follows the Varavel design system to the letter.

The project has a build pipeline: source files live in `src/` and are compiled to `static/`. CSS is processed by TailwindCLI, JS is bundled by esbuild, and the final site is generated by Zola.

- **Core stack**: Zola 0.22.1+, TailwindCSS v4, Alpine.js (bundled via esbuild), Geist Sans / Geist Mono
- **Purpose**: All-in-one theme for Varavel projects

## Maintaining this document

After completing any task, review this file and update it if you made structural changes or discovered patterns worth documenting. Only add information that helps understand how to work with the project. Avoid implementation details, file listings, or trivial changes. This is a general guide, not a changelog nor a journal.

When updating this document, keep the full document context in mind; do not simply append new sections at the end, but place them where they make the most sense within the document.

## Mandatory reading before any change

**Before making ANY changes to this project**, agents MUST read and understand these two documents:

1. **Brand guidelines**: https://raw.githubusercontent.com/varavelio/brand/refs/heads/main/README.md
2. **Design system**: https://raw.githubusercontent.com/varavelio/brand/refs/heads/main/DESIGN.md

These documents contain the Varavel brand guidelines and design system. Every visual and design decision must align with them.

## Tech stack

| Layer                 | Technology                                 | Version/Detail                       |
| --------------------- | ------------------------------------------ | ------------------------------------ |
| Static Site Generator | **Zola**                                   | `>= 0.22.1`                          |
| CSS                   | **TailwindCSS**                            | `4.3.0`                              |
| CSS Plugin            | **@tailwindcss/typography**                | `0.5.19`                             |
| Interactivity         | **Alpine.js**                              | `3.15.12` (npm, bundled via esbuild) |
| JS Bundler            | **esbuild**                                | `0.28.0`                             |
| Dev Runner            | **concurrently**                           | `9.2.1`                              |
| Formatting            | **dprint**                                 | `0.54.0`                             |
| Typography            | **Geist Sans** / **Geist Mono**            | Self-hosted at `static/fonts/geist/` |
| Syntax highlighting   | github-light-default / github-dark-default | With extra VDL grammar               |

## Architecture & Organization

### Root Layout

- `src/` — Canonical source files for CSS and JS (the source of truth).
- `static/` — Built/generated output (CSS, JS, fonts, icons). Do not edit files here directly.
- `templates/` — Zola Tera templates (page layouts, macros, shortcodes).
- `content/` — Zola content pages (markdown).
- `scripts/` — Utility scripts (font downloads, icon downloads).
- `grammars/` — Extra Zola syntax highlighting grammars (VDL).
- `zola.toml` — Zola site configuration.
- `theme.toml` — Zola theme metadata.
- `package.json` — Node.js project configuration and scripts.
- `dprint.json` — Code formatting configuration.

### `src/` (Canonical Source)

Source files are compiled to `static/` during the build. Editing `src/` is the correct way to change CSS and JS.

- **`src/css/`** — CSS source files
  - `varapress.css` — Main CSS entry point. Imports TailwindCSS, defines theme tokens, dark mode overrides, base layer styles, and the other CSS files below.
  - `alpine.css` — Alpine.js `[x-cloak]` rule.
  - `docs.css` — Docs-specific styles: sidebar tree lines (dashed connectors), anchor link styling on prose headings.
  - `giallo.css` — Zola syntax highlighter styles (`.giallo-l`, `.giallo-ln` classes).
- **`src/js/`** — JavaScript source files
  - `theme-init.js` — Theme bootstrapper: FOUC prevention, `window.__varapressTheme` runtime API, localStorage persistence, OS `prefers-color-scheme` listener.
  - `varapress/` — Alpine.js application code (bundled into `static/js/varapress.js`).
    - `index.js` — Entry point: imports Alpine, initializes docs, starts Alpine.
    - `docs.js` — Docs interactivity: `injectAnchorLinks()`, Alpine data component (`varapressDocs` with sidebar, TOC, and scroll observer).
    - `theme.js` — Standalone Alpine theme component (`theme` with `cycleTheme()`, reactive `theme`/`resolvedTheme`).

### `templates/` (Zola Templates)

Root templates are thin delegators that extend `base/main.html` and fill blocks via `{% include %}`. Actual content lives in subdirectories:

```
templates/
  base/main.html              → Base HTML shell, blocks, script loading
  docs.html                   → Delegator (extends base, includes docs/ blocks)
  docs/
    head.html                 → SEO meta overrides (per-page description, OG)
    header.html               → Header (logo, theme toggle, hamburger, GitHub button)
    sidebar.html              → Desktop + mobile sidebar nav (uses sidebar-nav-tree.html)
    sidebar-bg.html           → Background fill for the desktop sidebar area
    sidebar-nav-tree.html     → Recursive tree of sections and pages in sidebar
    content.html              → Breadcrumbs, prose content, prev/next pagination
    toc.html                  → Desktop + mobile TOC (with floating mobile button)
  section.html                → Delegator (default: extends docs.html)
  page.html                   → Delegator (default: extends docs.html)
  index.html                  → Home page (extends landing.html)
  landing.html                → Landing page layout (full-width, no sidebar/TOC)
  blog.html                   → Blog layout (TODO: placeholder, extends base/main.html)
  macros/                     → Reusable Tera macros
    alert.html                → Status alert banner (info/success/warning/error)
    badge.html                → Compact status badge (soft/solid/outline)
    button.html               → Versatile button (supports href, icons, variants)
    github_button.html        → GitHub repo button with live stars/forks/tag
    heading.html              → Semantic heading with visual size presets
    icons.html                → Core icon engine (Lucide + Simple Icons)
    kbd.html                  → Keyboard key indicator
  shortcodes/                 → Zola shortcodes for Markdown content
    icon.html                 → Inline icon in markdown (wrapper around icons macro)
    button_base.html          → Base button shortcode backed by the button macro
    comparison_table.html     → Product/alternative comparison table section
    container.html            → Centered content container
    cta_banner.html           → Call-to-action banner section
    cta_inline.html           → Compact inline call-to-action section
    faq_accordion.html        → FAQ accordion section
    features_grid.html        → Feature cards grid section
    features_list.html        → Split feature list section
    feature_tabs.html         → Alpine-powered tabbed feature showcase
    footer_simple.html        → Simple footer section
    header_base.html          → Base header section
    hero_centered.html        → Centered hero section with title/actions
    hero_split.html           → Split hero section with proof/visual panel
    lead_capture_card.html    → Lead capture form card section
    logo_cloud_grid.html      → Logo cloud / trust bar section
    pricing_cards.html        → Pricing cards section
    stats_grid.html           → Statistics grid section
    steps_cards.html          → Step-by-step cards section
    testimonials_grid.html    → Testimonial cards section
```

### Template routes and inheritance

- `section.html` and `page.html` both extend `docs.html` by default (thematic docs layout). Users can override via frontmatter `template = "..."`.
- `index.html` extends `landing.html`.
- `landing.html` extends `base/main.html` with a full-width layout (no sidebar, no TOC).
- `blog.html` is a placeholder (TODO, extends `base/main.html`).

### Landing shortcode conventions

- Full-width landing sections keep the outer section full width for backgrounds and borders, then center their inner content with a `container` parameter (`xs`, `sm`, `md`, `lg`, `xl`, `full`).
- The `container` block shortcode remains for arbitrary Markdown/custom content. Do not wrap standard landing section shortcodes just to constrain width; set `container="..."` on the section instead.
- Mobile spacing in landing cards should stay compact (`p-4` base, larger only behind `desk:`) to preserve usable mobile layouts.
- Small Alpine interactions can live inline in shortcode HTML when they remain local to the component (for example menus, accordions, tabs, or no-backend lead form confirmations).

## Script loading

JavaScript is loaded differently depending on the file:

1. **`theme-init.js`** — Built from `src/js/theme-init.js` → `static/js/theme-init.js`. **Inlined** in `<head>` via `load_data` with `format="plain"` for synchronous FOUC prevention before the first paint.
2. **`varapress.js`** — Built from `src/js/varapress/index.js` (bundling `docs.js` and Alpine.js) → `static/js/varapress.js`. Loaded via `<script src="..." defer>` in `<head>`.

```tera
{# theme-init.js: FOUC prevention, runs synchronously in <head> #}
<script>{{ load_data(path="js/theme-init.js", format="plain") | safe }}</script>

{# varapress.js: Alpine.js + docs interactivity, loaded deferred #}
<script src="{{ get_url(path='js/varapress.js') }}" defer></script>
```

**Key distinction**: `theme-init.js` is the only file that is inlined. All other JS (`varapress.js`) uses standard `<script>` tags.

The `src/js/` files are the canonical, documented source. `static/js/` contains built output — do not edit it directly.

Use as little JavaScript as possible, limit it to when it's really necessary, and prioritize keeping it short and focused. JavaScript should be loaded only where it's used.

## Theme initialization

1. **FOUC prevention** — `src/js/theme-init.js` is built to `static/js/theme-init.js` and inlined in `<head>`. It runs synchronously before the first paint.
2. **Runtime API** — Exposes `window.__varapressTheme` with `.get()` and `.set(theme)` methods. Alpine.js components sync with it via the `varapress-theme-change` custom event.
3. **Bootstrap flow**: read `localStorage` → resolve `"system"` via `matchMedia` → apply `data-theme` + `dark` class → emit custom event → listen for OS-level changes and cross-tab storage events.

## Anchor links on headings

Prose headings in documentation pages automatically get anchor links with a chain-link icon. The icon appears on hover and allows users to copy direct links to specific sections.

**How it works:**

- `src/js/varapress/docs.js` — `injectAnchorLinks()` scans `.prose :is(h1-h6)` elements with an `id` attribute and prepends an `<a class="anchor-link">` with a chain-link SVG icon. This is bundled into `varapress.js` via esbuild.
- `src/css/docs.css` — Styles the anchor link as absolutely positioned (via inline) to the left of the heading, invisible by default, fading in on hover. Also includes sidebar tree-line utilities (`tree-line-v`, `tree-line-h`). Imported from `varapress.css`.
- Scripts are loaded globally (not just on docs pages) — anchor link injection is a no-op when no matching headings exist.
- The URL hash updates via `history.pushState` without page reload.
- Accessibility: icon is `aria-hidden` and focusable only via keyboard.

**When modifying:** The SVG icon is inlined in `docs.js` (not loaded from `static/icons/`). If you need to change the icon, update the `LINK_SVG` constant in `injectAnchorLinks`. Docs-specific CSS lives in `src/css/docs.css`, not in `varapress.css`.

## Component macros

Tera macros are available in `templates/macros/`. Each file exposes a `render` macro.

**Shared conventions:**

- All parameters use strings (`"true"` / `"false"` for booleans).
- `color` accepts: `neutral` (default), `info`, `success`, `warning`, `error`.
- `size` defaults to `"md"` unless noted.
- `class` appends extra CSS classes to any macro.

Available macros:

| File                 | Purpose                                                                        |
| -------------------- | ------------------------------------------------------------------------------ |
| `icons.html`         | Core icon engine: auto-detects Lucide vs Simple Icons, injects `fill`/`stroke` |
| `button.html`        | Versatile button with href, icon, size, radius, variant, color                 |
| `badge.html`         | Compact status indicator (soft/solid/outline variants)                         |
| `alert.html`         | Static status alert banner (info/success/warning/error)                        |
| `github_button.html` | GitHub repo link with live stars, forks, tag badges                            |
| `heading.html`       | Semantic heading with visual size presets independent of HTML level            |
| `kbd.html`           | Keyboard key indicator for shortcut display                                    |

## Icons

Icons come from two sources:

- **Lucide** — general-purpose line icons (e.g., `house`, `bell`, `search`)
- **Simple Icons** — brand/platform logos (e.g., `github`, `x`, `discord`)

They are downloaded to `static/icons/` by `scripts/download-icons.sh` and rendered through two entry points:

| Context             | Usage                                              | File                             |
| ------------------- | -------------------------------------------------- | -------------------------------- |
| Templates (`.html`) | `{{ icons::render(name="bell", class="size-5") }}` | `templates/macros/icons.html`    |
| Markdown content    | `{{ icon(name="github") }}`                        | `templates/shortcodes/icon.html` |

**IMPERATIVE rules for working with icons:**

1. **Only use icons already included in the project.** Never download, import, create new or reference external icons. The project ships with thousands of icons — if the one you need isn't there, ask first.
2. **To find an icon**, search inside `static/icons/`. With 5000+ files, browsing is impractical — use your search tools (grep/glob by name) to locate the exact `.svg` file.
3. **The icon name** is the filename without the `.svg` extension. For example, `static/icons/github.svg` becomes `name="github"`.
4. **The macro auto-detects** whether an icon is a Lucide line icon (`fill="none"`) or a Simple Icons solid icon, and applies the correct `fill`/`stroke` attributes automatically.

## Project conventions

### Design & CSS

1. **Use `src/css/varapress.css` tokens** — Do not hardcode arbitrary values. Use Tailwind utility classes that reference the defined CSS variables (e.g., `bg-base-100`, `text-content`, `rounded-md`, `font-medium`).
2. **Follow Varavel's design guide** — The brand/design documents are binding. Any new interface must comply with those guidelines.
3. **Dark mode** — Use `dark:` prefix or variant. The project supports both class and attribute (`dark:` variant in Tailwind is already configured).
4. **Responsive** — Mobile-first with a single breakpoint: `desk` at `64rem`. Use `desk:` prefix.
5. **Prose** — The `@tailwindcss/typography` plugin is available for rich text surfaces. Use the `prose` class where appropriate.

### Code

1. **No Sass** — `compile_sass = false` in `zola.toml`. Do not use `.scss`/`.sass` files.
2. **CSS source** is `src/css/varapress.css` — it imports Tailwind, defines theme tokens, and imports `alpine.css`, `giallo.css`, and `docs.css`.
3. **JS source** is `src/js/varapress/index.js` — it imports Alpine.js and `docs.js`, bundles everything into `static/js/varapress.js`.
4. **Theme init source** is `src/js/theme-init.js` — built separately to `static/js/theme-init.js`.

## Testing & quality

- **Linting**: `npm run lint` runs `dprint check` (JSON, Markdown, TOML, CSS, YAML) and `zola check`
- **Tests**: `npm run test` runs Node.js unit tests with the built-in `node:test` runner. Source-level tests may live near their source files.
- **Build**: `npm run build` compiles CSS, bundles JS, builds theme-init, then runs Zola build
- **CI**: `npm run ci` runs lint, tests, then build (used for CI validation)
- **Format**: `npm run format` runs `dprint fmt`
- **Local development**: `npm run dev` uses `concurrently` to run CSS watcher, JS watcher, and Zola dev server simultaneously

## Commands

| Command          | Purpose                                          |
| ---------------- | ------------------------------------------------ |
| `npm run build`  | Full build: CSS → JS → theme-init → Zola         |
| `npm run dev`    | Dev server with hot-reload for CSS, JS, and Zola |
| `npm run lint`   | `dprint check` + `zola check`                    |
| `npm run test`   | Run Node.js unit tests                           |
| `npm run format` | `dprint fmt` auto-formatting                     |
| `npm run ci`     | `npm run lint && npm test && npm run build`      |

Always read `package.json` to see the full set of available commands.

## General instructions

It's imperative that you follow the following instructions:

1. **Always read the brand documents first** before proposing design changes.
2. **Use `src/css/varapress.css` tokens** — it is the source of truth for the design system.
3. **Run `npm run lint` before finalizing** any task.
4. **Run `npm run build` to ensure** the project compiles correctly.
5. **Compiled CSS lives at `static/css/varapress.css`** — source is `src/css/varapress.css`, do not edit the compiled output directly.
6. **Compiled JS lives at `static/js/varapress.js`** — source is `src/js/varapress/index.js`, do not edit the compiled output directly.
7. **Geist fonts are downloaded** with the `scripts/download-fonts.sh` script and used in `src/css/varapress.css`.
8. **Context7 MCP** — If the Context7 MCP server is available, use it at any time to look up up-to-date documentation and information about Zola or any other tool in the stack (TailwindCSS, Alpine.js, esbuild, etc.).
9. **Run `npm run ci` after changes** — After making any changes, always run `npm run ci` to verify all checks pass. If there are errors, fix them before finishing the task.
10. **Complete tasks 100%** — When assigned a task, complete it 100%. Do not stop until the task is fully finished.
